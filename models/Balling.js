class Balling{
    constructor(balling_id, test_id, balling_number, balling_min_value, balling_max_value, balling_auto_result) {
        this.balling_id = balling_id
        this.test_id = test_id
        this.balling_number = balling_number
        this.balling_min_value = balling_min_value
        this.balling_max_value = balling_max_value
        this.balling_auto_result = balling_auto_result
    }
}

module.exports = Balling